{{- if .Values.postgres.enabled }}
{{- $secretName := include "cbctf.postgres.fullname" . }}
{{- $existing := lookup "v1" "Secret" .Release.Namespace $secretName }}
{{- $pwd := .Values.postgres.auth.password }}
{{- if not $pwd }}
  {{- if and $existing $existing.data (index $existing.data "postgres-password") }}
    {{- $pwd = index $existing.data "postgres-password" | b64dec }}
  {{- else }}
    {{- $pwd = randAlphaNum 20 }}
  {{- end }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "cbctf.postgres.labels" . | nindent 4 }}
type: Opaque
stringData:
  postgres-password: {{ $pwd | quote }}
{{- end }}
